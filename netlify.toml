[build]
  command = "python -m pip install --upgrade pip setuptools wheel && python -m pip install zensical && python -m zensical.main build"
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.12"
